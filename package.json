{
  "private": true,
  "workspaces": {
    "packages": ["packages/*"]
  },
  "scripts": {
    "build": "yarn workspace demo-utils build && yarn workspace runner build",
    "start": "yarn workspace runner run start",
    "prettier": "yarn workspaces run prettier"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mofeiZ/uie-conf.git"
  }
}